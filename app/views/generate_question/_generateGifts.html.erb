<div class="h_text">
	<!-- <img alt="" src="images/1-t1.jpg" />--><h1>Selecione os presentes que mais se encacham com você!</h1><br />
</div>
<div class="padding">
	<div class="main_content">
		<div class="show">
			<table id="gifts" class="grid">
				<thead>
					<tr>
						<th>Nome</th>
						<th>Adicionar</th>
					</tr>
				</thead>
				<tbody>
					<% @allGifts.each do |gift| %>
						<tr id="<%= dom_id gift %>" class="show_grid">
							<td><%= gift.name %></td>
						    <td id="add_<%= gift.id %>"><%= link_to 'Adicionar', gift, :remote => true %></td>
							<td id="remove_<%= gift.id %>" style="display:none"><%= link_to 'Remover', gift, :remote => true %></td>
						</tr>
					<% end %>
				</tbody>
				<tfooter>

				</tfooter>
			</table>
		</div>
	</div>
</div>