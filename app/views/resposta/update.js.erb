$("#<%= dom_id(@respostum) %>").replaceWith("<%= escape_javascript(render(:partial => @respostum)) %>");
<% @respostum = Respostum.new # reset for new form %>
$(".edit_respostum").replaceWith("<%= escape_javascript(render(:partial => "form"))%>")
$(".new_respostum")[0].reset();