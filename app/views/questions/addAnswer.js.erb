if('<%= @first %>' == 'true') {
	$("#addAnswer").remove();
	$("<%= escape_javascript(render(:partial => "addAnswer"))%>").prependTo("#<%= dom_id(@question) %>");
} else {
	$("#addAnswer").empty();
	alert('Resposta adicionada com sucesso.');
}
