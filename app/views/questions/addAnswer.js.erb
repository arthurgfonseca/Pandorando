if('<%= @first %>' == 'true') {
	$("#addAnswer").replaceWith("<%= escape_javascript(render(:partial => "addAnswer"))%>")
} else {
	$("#addAnswer").empty();
	alert('resposta adicionada com sucesso')
}
