$("#<%= dom_id(@category) %>").replaceWith("<%= escape_javascript(render(:partial => @category)) %>");
<% @category = Category.new # reset for new form %>
$(".edit_category").replaceWith("<%= escape_javascript(render(:partial => "form"))%>")
$(".new_category")[0].reset();