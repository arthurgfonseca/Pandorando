<%= form_for(@presente) do |f| %>
  <% if @presente.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@presente.errors.count, "error") %> prohibited this presente from being saved:</h2>

      <ul>
      <% @presente.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br />
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
