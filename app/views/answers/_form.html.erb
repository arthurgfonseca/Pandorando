<%= form_for(@answer, :remote => true) do |f| %>
  <% if @answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer.errors.count, "error") %> prohibited this answer from being saved:</h2>

      <ul>
      <% @answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p class="form_field">
    <label class="label"><%= f.label :enunciado %></label>
    <label class="text_field"><%= f.text_field :enunciation %></label>
  </p>
  <div class="buttons">
    <a><%= f.submit "Salvar" %></a>
	<a class="cancel" onclick="$('.addAnswer').remove();">Cancelar</a>
  </div>
<% end %>