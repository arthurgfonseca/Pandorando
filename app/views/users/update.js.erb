$("#<%= dom_id(@user) %>").replaceWith("<%= escape_javascript(render(:partial => @user)) %>");
<% @user = User.new # reset for new form %>
$(".edit_user").replaceWith("<%= escape_javascript(render(:partial => "form"))%>")
$(".new_user")[0].reset();